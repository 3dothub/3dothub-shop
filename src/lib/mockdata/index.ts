import { products } from "@/lib/mockdata/products";
import { sections } from "@/lib/mockdata/section";
import { shopCategories } from "@/lib/mockdata/shopCategories";
import { specialOccasions } from "@/lib/mockdata/specialOccasion";
import { subCategories } from "@/lib/mockdata/subCategories";

export const mockDataCollections = {
  products,
  sections,
  shopCategories,
  specialOccasions,
  subCategories,
};
